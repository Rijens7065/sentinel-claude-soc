name: Test Runner

on:
  push:
    branches:
      - "phase-*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Smoke Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo
        run: echo "Workflow runner is working"
